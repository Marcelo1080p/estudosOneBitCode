let spaceship = ['Colosus', 'Helmet', 'Elemental']

let elementalPos = spaceship.indexOf("Golias")
if(elementalPos == -1){
    console.log('Valor não existente')
} else {
    console.log(elementalPos)
}
